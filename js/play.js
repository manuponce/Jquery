/**
 * Inicializa variables para los elementos pasos, contenedor, elementos, anclajes, reinicio y reproducción.
 * @var {jQuery} pasos: el objeto jQuery que representa el elemento pasos.
 * @var {jQuery} contenedor: el objeto jQuery que representa el elemento contenedor.
 * @var {jQuery} items: el objeto jQuery que representa el elemento items.
 * @var {jQuery} anclajes: el objeto jQuery que representa el elemento anclajes.
 * @var {jQuery} reset: el objeto jQuery que representa el elemento del botón de reinicio.
 * @var {jQuery} play: el objeto jQuery que representa el elemento del botón de reproducción.
 */
var steps = $('.steps'), wrapper = $('.cn-wrapper'), items = $('.cn-wrapper li'), anchors = $('.cn-wrapper li a'), reset = $('.reset-button'), play = $('.play-button');
